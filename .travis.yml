language: node_js
node_js:
  - lts/*
install:
  - npm install # npm ci
script:
  - npm run docs:build # npm run docs:build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist
  github_token: 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 # https://github.com/settings/tokens
  keep_history: true
  on:
    branch: main